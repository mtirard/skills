#!/usr/bin/env bash
# Logged wrapper for grep — pre-approved for /opt/wl-docs exploration.
# Only logs when a doc path is involved; pipeline stages and unrelated
# greps pass through silently.
if [[ "$*" == */opt/wl-docs/* ]]; then
    printf '%s grep %s\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" "$*" >> /opt/wl-docs/usage.log 2>/dev/null
fi
exec /usr/bin/grep "$@"
