#!/usr/bin/env bash
# Logged wrapper for sed — pre-approved for /opt/wl-docs exploration
printf '%s sed %s\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" "$*" >> /opt/wl-docs/usage.log 2>/dev/null
exec /usr/bin/sed "$@"
